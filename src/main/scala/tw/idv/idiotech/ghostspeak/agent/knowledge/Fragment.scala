package tw.idv.idiotech.ghostspeak.agent.knowledge

class Fragment {

// val test: Graph[State, LkDiEdge]
  // trigger: perception => action, internal state change
  // for plans: internal state change =
  // create new trigger =>
  // Memory = all internal states
  // Loop:

  // perception actor => send raw message to state update
  // state update => send message to action manager
  // action manager => carry out action

}
